<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyMauiApp.MainPage">

    <Grid Padding="30" RowDefinitions="Auto,Auto,Auto,Auto,*" RowSpacing="20">
        <Entry x:Name="firstNameEntry" Placeholder="First Name" Grid.Row="0" />
        <Entry x:Name="lastNameEntry" Placeholder="Last Name" Grid.Row="1" />
        <Button Text="Save User" Clicked="OnSaveUserClicked" Grid.Row="2" />
        <Label Text="Saved Users:" FontAttributes="Bold" Grid.Row="3" />

        <CollectionView x:Name="usersCollection"
                        Grid.Row="4"
                        SelectionMode="Single"
                        SelectionChanged="OnUserSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Vertical" Spacing="5" Padding="10" BackgroundColor="#f0f0f0">
                        <Label Text="{Binding FirstName}" />
                        <Label Text="{Binding LastName}" />
                        <Button Text="Edit" Clicked="OnEditClicked" CommandParameter="{Binding}" />
                        <Button Text="Delete" Clicked="OnDeleteClicked" CommandParameter="{Binding}" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
